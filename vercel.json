{
    "rewrites": [{ 
        "source": "/(.*)",
        "destination": "/" }],
    "git": {
        "deploymentEnabled": {
            "$VERCEL_GIT_COMMIT_REF": "[[\"$VERCEL_GIT_COMMIT_REF\" == \"main\"] && true || false"
        }
    }
}
  